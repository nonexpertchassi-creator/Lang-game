# 🌍 언어 학습 도트 게임 — 개발 계획서

> 가칭: **LinguaTrip** (추후 변경 가능)
> 한 줄 소개: 도트 그래픽 세계를 여행하며 그 나라에서 "살아남는 데 필요한" 언어를 배우고,
> 배운 언어가 제2, 제3, 제4 외국어로 가지처럼 뻗어나가는 웹 게임.

---

## 1. 게임 컨셉

### 핵심 아이디어
- 플레이어는 배낭여행자. 도트(픽셀아트) 맵 위에서 나라를 이동한다.
- 각 나라에 도착하면 **실전 여행 회화** 중심의 미니게임/퀘스트로 언어를 배운다.
  - 예: 공항 입국 심사 통과하기, 식당에서 주문하기, 길 묻기, 흥정하기
- 한 언어를 일정 수준 마스터하면 **연결된 나라(언어)로 가는 항공권이 해금**된다.
  - 예: 영어 → 스페인어/프랑스어 해금 → 포르투갈어/이탈리아어 해금 …
  - 언어 계통(로망스어군, 게르만어군 등)을 "노선도"처럼 시각화 → 제2, 3, 4 외국어로 자연스럽게 확장
- 학습 = 게임 진행. 진행 = 새 지역 탐험. (듀오링고의 학습 루프 + 여행 RPG의 탐험 보상)

### 핵심 루프 (Core Loop)
```
나라 도착 → 퀘스트(상황극) 수행 → 단어/문장 학습 퀴즈
→ 보상(코인·스탬프·경험치) → 복습(간격 반복) → 다음 지역/언어 해금
```

### 차별점
- 단순 카드 암기가 아니라 **상황(Context) 기반**: NPC와의 도트 대화 장면 속에서 학습
- **언어 노선도(Language Map)**: 언어 간 유사성을 게임 시스템으로 활용
  (스페인어를 알면 이탈리아어 단어 일부가 "이미 아는 단어"로 표시 → 학습 동기 부여)
- 여권/스탬프 수집 등 수집 욕구 자극

---

## 2. MVP 범위 (1차 출시 목표)

작게 시작해서 빨리 배포하고, 광고를 붙인 뒤 콘텐츠를 늘린다.

### 포함
- [ ] 도트 월드맵 1개 (시작: 한국 → 첫 여행지: 일본 또는 영어권 1개국)
- [ ] 언어 2개 (예: 영어, 일본어) × 각 3개 챕터 (공항 / 식당 / 길찾기)
- [ ] 학습 미니게임 3종
  1. 단어 매칭 (그림 ↔ 단어)
  2. 문장 조립 (어순 배열)
  3. 대화 선택지 (NPC 상황극)
- [ ] 진행 저장 (localStorage → 추후 계정 연동)
- [ ] 간격 반복 복습 시스템 (간단한 Leitner 박스 방식)
- [ ] 광고 1종 이상 부착 (아래 §5)
- [ ] 모바일 브라우저 대응 (반응형 + 터치 조작)

### 제외 (2차 이후)
- 음성 인식/발음 평가, 멀티플레이, 계정/서버, 리더보드, 모바일 앱 패키징

---

## 3. 기술 스택

| 영역 | 선택 | 이유 |
|---|---|---|
| 게임 엔진 | **Phaser 3** + TypeScript | 웹 2D 도트 게임 표준. 타일맵·스프라이트·씬 관리 내장 |
| 빌드 | Vite | 빠른 개발 서버, 간단한 정적 빌드 |
| UI(퀴즈/메뉴) | HTML/CSS 오버레이 | 텍스트 많은 학습 UI는 캔버스보다 DOM이 유리 (다국어 폰트, 접근성, SEO) |
| 데이터 | JSON 콘텐츠 파일 | 언어/레슨 데이터를 코드와 분리 → 콘텐츠 추가가 곧 업데이트 |
| 저장 | localStorage | 서버 없이 시작. 추후 Supabase 등으로 확장 |
| 배포 | GitHub Pages 또는 Vercel | 무료 정적 호스팅, 도메인 연결 가능 |
| 도트 에셋 | 무료 에셋(Kenney, itch.io CC0) + 자체 제작 | 초기 비용 0원 |

서버가 전혀 필요 없는 **100% 정적 사이트**로 시작 → 호스팅 비용 0원, 광고 수익이 곧 순수익.

---

## 4. 콘텐츠 구조 (데이터 설계)

```
src/data/
  languages.json        # 언어 노선도: 언어 간 해금 관계(그래프)
  ko/                   # 학습자 모국어(UI 언어)
    en/                 # 대상 언어
      chapter-airport.json
      chapter-restaurant.json
      chapter-directions.json
    ja/
      ...
```

레슨 항목 예시:
```json
{
  "id": "en-airport-001",
  "type": "phrase",
  "target": "Where is the baggage claim?",
  "meaning": "수하물 찾는 곳이 어디예요?",
  "context": "공항 도착 후 NPC에게 질문",
  "cognates": { "es": "equipaje" }
}
```
- `cognates` 필드로 언어 간 연결 구현 → "이미 아는 단어" 시스템의 기반

---

## 5. 수익화 전략

### 1단계 — 출시 직후
- **Google AdSense for Games (H5 Games Ads / Ad Placement API)**
  - 웹 게임 전용 광고: 전면 광고(챕터 사이) + **보상형 광고**(힌트 보기, 하트 회복, 복습 2배 보상)
  - 보상형 광고가 학습 게임과 궁합이 가장 좋음 (사용자가 자발적으로 시청)
- 일반 **AdSense 디스플레이 광고**: 게임 외 영역(레슨 결과 화면 하단 등)에 배치
- ⚠️ 승인 요건: 자체 도메인 + 어느 정도의 콘텐츠/트래픽 필요 → 도메인 1개 구입 권장(연 1~2만원)

### 2단계 — 트래픽 확보 후
- 광고 제거 + 추가 콘텐츠 잠금해제 **프리미엄 패스** (일회성 결제)
- 모바일 앱 패키징(Capacitor) 후 **AdMob** 추가 → 수익원 다변화

### 트래픽 전략 (광고 수익 = 트래픽이 전부)
- SEO: 레슨 콘텐츠를 정적 페이지로도 노출 ("여행 일본어 50문장" 류 검색 유입)
- 짧은 플레이 영상(도트 감성) → 쇼츠/틱톡 공유 유도
- itch.io, CrazyGames 등 웹게임 포털 동시 배포로 초기 유저 확보

---

## 6. 로드맵

| 단계 | 기간(목표) | 내용 |
|---|---|---|
| **Phase 0** | 1주 | 프로젝트 셋업(Vite+Phaser+TS), 도트 맵에서 캐릭터 이동, 배포 파이프라인 |
| **Phase 1** | 2~3주 | 학습 미니게임 3종 + 첫 언어(영어) 챕터 3개 + 진행 저장 |
| **Phase 2** | 1~2주 | 두 번째 언어 + 언어 노선도 화면 + 복습 시스템 |
| **Phase 3** | 1주 | 도메인 연결, AdSense 신청, 보상형 광고 연동, 정식 공개 |
| **Phase 4** | 지속 | 콘텐츠 확장(언어/챕터 추가), 지표 보고 개선, 프리미엄 패스 |

---

## 7. 바로 다음 할 일

1. 프로젝트 스캐폴딩 (Vite + Phaser 3 + TypeScript)
2. 걸어다닐 수 있는 도트 월드맵 프로토타입 1장
3. 단어 매칭 미니게임 1종 + 영어 공항 챕터 데이터
4. GitHub Pages 자동 배포 설정
